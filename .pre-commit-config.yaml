# Sample pre-commit configuration
# To use: pip install pre-commit && pre-commit install

repos:
  - repo: local
    hooks:
      - id: defsort
        name: defsort
        entry: uv run defsort
        language: system
        types: [python]
